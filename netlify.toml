[build]
  command = "npm install && cd backend && npm install && cd .. && npm run build"
  publish = "dist"
  functions = "backend"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/index/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
